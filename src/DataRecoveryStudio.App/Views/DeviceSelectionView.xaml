<UserControl x:Class="DataRecoveryStudio.App.Views.DeviceSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:application="clr-namespace:DataRecoveryStudio.Application;assembly=DataRecoveryStudio.Application">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="40,34,40,44" MaxWidth="1180" HorizontalAlignment="Center">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid>
                <StackPanel>
                    <TextBlock Text="{Binding DataContext.Localization.DeviceTitle, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource PageTitleStyle}" />
                    <TextBlock Text="{Binding DataContext.Localization.DeviceSubtitle, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource PageSubtitleStyle}" Margin="0,9,0,0" MaxWidth="720" />
                </StackPanel>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" AutomationProperties.Name="Refresh mock devices" />
            </Grid>
            <Border Grid.Row="1" Margin="0,28,0,22" Background="{DynamicResource AccentSubtleBrush}" CornerRadius="10" Padding="16,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ⓘ" Foreground="{DynamicResource AccentTextBrush}" FontSize="16" VerticalAlignment="Center" />
                    <TextBlock Text="Safe preview: all devices below are generated mock records. No disk enumeration or access occurs."
                               Margin="10,0,0,0" Foreground="{DynamicResource AccentTextBrush}" TextWrapping="Wrap" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Devices}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type application:DeviceCardViewModel}">
                        <Border Style="{StaticResource PanelStyle}" Width="365" Margin="0,0,18,18" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="48" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <Border Width="42" Height="42" CornerRadius="11" Background="{DynamicResource AccentSubtleBrush}" VerticalAlignment="Top">
                                        <TextBlock Text="▰" FontSize="20" Foreground="{DynamicResource AccentTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="10,0,8,0">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Model}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <Border Grid.Column="2" Background="{DynamicResource PositiveSubtleBrush}" CornerRadius="9" Padding="8,4" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Status}" FontSize="10" Foreground="{DynamicResource PositiveBrush}" FontWeight="SemiBold" />
                                    </Border>
                                </Grid>
                                <ProgressBar Grid.Row="1" Value="{Binding UsedPercentage}" Maximum="100" Margin="0,22,0,16" />
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                                    <StackPanel><TextBlock Text="CAPACITY" Style="{StaticResource MetricLabelStyle}" /><TextBlock Text="{Binding Capacity}" FontWeight="SemiBold" Margin="0,4,0,0" /></StackPanel>
                                    <StackPanel Grid.Column="1"><TextBlock Text="FREE" Style="{StaticResource MetricLabelStyle}" /><TextBlock Text="{Binding Free}" FontWeight="SemiBold" Margin="0,4,0,0" /></StackPanel>
                                    <StackPanel Grid.Column="2"><TextBlock Text="FORMAT" Style="{StaticResource MetricLabelStyle}" /><TextBlock Text="{Binding FileSystem}" FontWeight="SemiBold" Margin="0,4,0,0" /></StackPanel>
                                </Grid>
                                <Grid Grid.Row="3" Margin="0,22,0,0">
                                    <StackPanel VerticalAlignment="Center"><TextBlock Text="{Binding Type}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" /><TextBlock Text="{Binding MountPath}" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="11" Margin="0,3,0,0" /></StackPanel>
                                    <Button Content="Choose scan" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right"
                                            Command="{Binding DataContext.SelectDeviceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}" AutomationProperties.Name="Choose this mock device" />
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
